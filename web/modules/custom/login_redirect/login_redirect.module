<?php

/**
 * @file
 * Redirects users to /admin after login.
 *
 * Uses an event subscriber to intercept the redirect response
 * and change the destination from /user/[uid]?check_logged_in=1 to /admin.
 */
